query GET_TRANSACTIONS {
  transactions {
    id
    product_id
    user_id
    timestamp
  }
}

query GET_TRANSACTION($id: String!) {
  transaction(id: $id) {
    id
    product_id
    user_id
    timestamp
  }
}
